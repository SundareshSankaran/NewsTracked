{
  "name": "restaf",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx restaf-server --env=auth.env",
    "indocker": "npx restaf-server",
    "buildapp": "docker build -f Dockerfile -t ssapp .",
    "runapp": "docker run -p 5000:8080  --env-file overrides.env  -t ssapp"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "restaf-server": "^6.9.0"
  }
}
